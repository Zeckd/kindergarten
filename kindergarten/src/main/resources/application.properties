spring.application.name=kindergarten
spring.jackson.serialization.write-dates-as-timestamps=false

# PostgreSQL Configuration (Commented out due to Docker issues)
# spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:5432/db
# spring.datasource.username=postgres
# spring.datasource.password=isken
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# H2 Configuration (Temporary fallback)
spring.datasource.url=jdbc:h2:mem:kindergarten_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

server.port=8081